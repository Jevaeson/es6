var b = require('./button.js')
// 导入自定义模块的方法
// require('路径')
// 导入第三方模块和核心模块
// require('模块名')
const $ = require('jquery')

$.each([1, 2, 3], function(ele) {
  console.log(ele)
})

console.log(b)
const a = 10
console.log(a)
